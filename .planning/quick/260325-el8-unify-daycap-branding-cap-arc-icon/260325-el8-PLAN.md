---
phase: quick
plan: 260325-el8
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/screens/TodayView.tsx
  - src/assets/daycap-logo.svg
  - src/assets/icon.png
  - src/assets/icon.icns
autonomous: true
must_haves:
  truths:
    - "Dock/app icon shows a rising arc/cap symbol with cyan-to-indigo gradient"
    - "In-app header shows the same arc/cap mark next to 'DayCap' text with gradient"
    - "Icon renders cleanly at all sizes (16px tray to 1024px icon)"
  artifacts:
    - path: "src/assets/icon.png"
      provides: "1024x1024 PNG app icon with arc/cap symbol"
    - path: "src/assets/icon.icns"
      provides: "macOS icon bundle with 16/32/128/256/512/1024 sizes"
    - path: "src/renderer/screens/TodayView.tsx"
      provides: "Inline SVG logo with unified arc/cap mark"
  key_links:
    - from: "src/main/index.ts"
      to: "src/assets/icon.png"
      via: "nativeImage.createFromPath"
      pattern: "icon\\.png"
---

<objective>
Unify DayCap branding so the dock/app icon and the in-app header logo use the same rising arc/cap symbol with cyan-to-indigo gradient.

Purpose: Consistent brand identity across all touchpoints (dock, tray, in-app header).
Output: New icon.png, icon.icns, and updated inline SVG in TodayView.tsx all sharing the same cap/arc mark.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/renderer/screens/TodayView.tsx
@src/main/index.ts
@src/assets/daycap-logo.svg
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate unified app icon (icon.png + icon.icns)</name>
  <files>src/assets/icon.png, src/assets/icon.icns</files>
  <action>
Create a Node.js script (temporary, delete after) that generates the app icon programmatically:

1. Install `canvas` as a dev dependency: `npm install --save-dev canvas`
2. Write a script `scripts/generate-icon.js` that:
   - Creates a 1024x1024 canvas
   - Fills background with dark color (#0f1117 or similar dark from the app's theme)
   - Draws the cap/arc symbol centered:
     - A horizontal brim line near the vertical center (slightly below)
     - A gentle upward arc (like a sunrise/cap brim) above the line
     - Both stroked with a linear gradient from cyan #06b6d4 (left) to indigo #6366f1 (right)
     - Stroke width ~60px for boldness at 1024px (scales down well)
     - Use rounded line caps for softness
   - The arc should be simple and geometric: a single smooth bezier or elliptical arc spanning about 60% of the canvas width, peaking about 25% above center
   - Add subtle rounded-rect clipping (like macOS icon shape) with ~180px corner radius
   - Exports as PNG to src/assets/icon.png
3. Run the script: `node scripts/generate-icon.js`
4. Generate icon.icns from the 1024px PNG using `sips` and `iconutil` (macOS built-in):
   - Create iconset directory with required sizes (16, 32, 128, 256, 512 at 1x and 2x)
   - `sips -z {size} {size} icon.png --out icon.iconset/icon_{size}x{size}.png` for each size
   - `iconutil -c icns icon.iconset -o src/assets/icon.icns`
5. Clean up: remove the temporary iconset directory and scripts/generate-icon.js
6. Uninstall canvas: `npm uninstall canvas`

The symbol design: think of a simple, bold arc — like the top curve of a baseball cap viewed from the front, or a sunrise arc. Just two elements: the arc curve and a flat brim line beneath it. No text in the icon, just the mark.
  </action>
  <verify>
    <automated>file src/assets/icon.png | grep -q "PNG image data, 1024 x 1024" && echo "icon.png OK" && file src/assets/icon.icns | grep -q "Mac OS X icon" && echo "icon.icns OK"</automated>
  </verify>
  <done>icon.png is a valid 1024x1024 PNG with the arc/cap symbol on dark background. icon.icns is a valid macOS icon bundle with all required sizes. No leftover temp files or dev dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Update in-app logo SVG to match the icon mark</name>
  <files>src/renderer/screens/TodayView.tsx, src/assets/daycap-logo.svg</files>
  <action>
Update the DayCapLogo component in TodayView.tsx (inline SVG, no file imports) to use the same arc/cap symbol from the icon:

1. Replace the current SVG paths (lines 19-21 in the existing DayCapLogo) with the unified mark:
   - A horizontal brim line (stroke, rounded caps)
   - A gentle upward arc above it (same proportions as the icon)
   - Both use the existing `url(#daycap-grad)` gradient (cyan to indigo)
   - The mark should fit in roughly a 24x24 viewBox area on the left side

2. Keep the "DayCap" text element to the right of the mark, using the same gradient fill.

3. Ensure the overall viewBox accommodates mark + text (approximately 140x36 or adjust as needed).

4. The arc in SVG terms: use a path with an elliptical arc command (A) or a quadratic bezier (Q). Example shape:
   - Brim: `M 2 20 L 22 20` (horizontal line)
   - Arc: `M 4 20 Q 12 6 20 20` (quadratic bezier curving up from left to right of brim)
   - Both with stroke="url(#daycap-grad)" strokeWidth="2.5" strokeLinecap="round" fill="none"

5. Also update src/assets/daycap-logo.svg to contain the same SVG markup (standalone version for reference).

The key change from current: replace the old cap-with-antenna shape with the cleaner arc-over-brim that matches the generated icon exactly.
  </action>
  <verify>
    <automated>cd /Users/ashwath/Desktop/Personal/AuctionX && grep -q "daycap-grad" src/renderer/screens/TodayView.tsx && grep -q "DayCap" src/renderer/screens/TodayView.tsx && echo "Logo SVG OK"</automated>
  </verify>
  <done>DayCapLogo component renders the same arc/cap mark as the app icon, followed by "DayCap" text, all with cyan-to-indigo gradient. The mark is simple, geometric, and visually identical to the icon at small sizes. daycap-logo.svg updated to match.</done>
</task>

</tasks>

<verification>
1. `file src/assets/icon.png` confirms valid 1024x1024 PNG
2. `file src/assets/icon.icns` confirms valid macOS icon bundle
3. `npm start` shows the new icon in dock and the matching logo in the header
4. The arc/cap symbol is visually consistent between dock icon and in-app header
</verification>

<success_criteria>
- App icon (dock, taskbar) displays the rising arc/cap symbol on dark background with cyan-to-indigo gradient
- In-app header shows the same arc/cap mark at ~24px alongside "DayCap" text with matching gradient
- icon.icns contains all required macOS icon sizes
- No leftover temporary scripts or dev dependencies from icon generation
</success_criteria>

<output>
After completion, create `.planning/quick/260325-el8-unify-daycap-branding-cap-arc-icon/260325-el8-SUMMARY.md`
</output>
