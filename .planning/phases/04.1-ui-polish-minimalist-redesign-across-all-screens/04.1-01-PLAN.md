---
phase: 04.1-ui-polish-minimalist-redesign-across-all-screens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/index.css
  - src/renderer/components/TaskRow.tsx
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-07
  - D-08
  - D-09
  - D-10
  - D-11
  - D-12

must_haves:
  truths:
    - "Task rows appear on clean white background with hairline bottom dividers, no filled row backgrounds"
    - "High-priority tasks show bold text only, no colored left border"
    - "Low-priority tasks show muted text color, no whole-row opacity dimming"
    - "Hover state on task rows is a subtle muted wash, not the accent fill"
    - "Border separators across the app are lighter (less saturated, higher lightness)"
    - "No text-primary or border-primary appears in src/renderer/ outside of App.tsx active nav tab and Button component (per D-05, D-06)"
  artifacts:
    - path: "src/renderer/index.css"
      provides: "Lightened --border CSS variable in :root"
      contains: "--border: 214.3 20% 93%"
    - path: "src/renderer/components/TaskRow.tsx"
      provides: "Minimalist task row with weight-only priority"
      contains: "bg-background"
  key_links:
    - from: "src/renderer/index.css"
      to: "src/renderer/components/TaskRow.tsx"
      via: "border-border class uses --border variable"
      pattern: "border-border"
---

<objective>
Apply the CSS variable border lightening and redesign TaskRow to use clean white backgrounds with weight-only priority signals.

Purpose: TaskRow is the most-used component and the border token affects all screens. These changes establish the visual foundation for the minimalist redesign.
Output: Lightened border token in index.css, redesigned TaskRow with bg-background, no left border priority, weight-only text signals.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-ui-polish-minimalist-redesign-across-all-screens/04.1-CONTEXT.md
@.planning/phases/04.1-ui-polish-minimalist-redesign-across-all-screens/04.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lighten --border CSS variable in index.css</name>
  <files>src/renderer/index.css</files>
  <read_first>
    - src/renderer/index.css
  </read_first>
  <action>
In `src/renderer/index.css`, in the `:root` block only (do NOT touch the `.dark` block):

Change line:
```
--border: 214.3 31.8% 91.4%;
```
To:
```
--border: 214.3 20% 93%;
```

This reduces saturation from 31.8% to 20% and increases lightness from 91.4% to 93%, producing a paler, less blue-tinted separator line. Per D-08.

Do NOT change any other CSS variables. Do NOT touch the `.dark` block's `--border: 217.2 32.6% 17.5%`.
  </action>
  <verify>
    <automated>grep -n "border: 214.3 20% 93%" src/renderer/index.css</automated>
  </verify>
  <acceptance_criteria>
    - src/renderer/index.css contains `--border: 214.3 20% 93%;` in the `:root` block
    - src/renderer/index.css still contains `--border: 217.2 32.6% 17.5%;` in the `.dark` block (unchanged)
    - src/renderer/index.css does NOT contain `--border: 214.3 31.8% 91.4%` (old value removed)
  </acceptance_criteria>
  <done>Light mode --border CSS variable updated to 214.3 20% 93%; dark mode untouched</done>
</task>

<task type="auto">
  <name>Task 2: Redesign TaskRow — clean background, weight-only priority, subtle hover</name>
  <files>src/renderer/components/TaskRow.tsx</files>
  <read_first>
    - src/renderer/components/TaskRow.tsx
    - src/renderer/screens/TodayView.tsx
  </read_first>
  <action>
In `src/renderer/components/TaskRow.tsx`, make these exact changes to the outer `div` className array:

1. **Row background (D-01):** Replace `bg-muted` with `bg-background`
2. **Hover (D-02):** Replace `hover:bg-accent` with `hover:bg-muted/40`
3. **Padding (D-04):** Replace `py-4` with `py-3`
4. **Remove left border for high priority (D-03, D-12):** Remove the entire second array entry:
   `task.priority === 'high' ? 'border-l-[3px] border-primary' : 'border-l-[3px] border-transparent'`
   Delete this line completely from the className array.
5. **Remove whole-row opacity for low priority (D-11):** Remove the entire third array entry:
   `task.priority === 'low' ? 'opacity-60' : ''`
   Delete this line completely from the className array.

The outer div className array should become a single string (no conditionals needed on the outer div):
```typescript
className="flex items-center gap-3 px-4 py-3 bg-background border-b border-border cursor-pointer hover:bg-muted/40"
```

6. **Title span priority classes (D-09, D-10, D-11):** Replace the current title `<span>` className array:

Current:
```typescript
className={[
  'flex-1 text-sm text-foreground',
  task.priority === 'high' ? 'font-semibold' : 'font-normal',
]
  .filter(Boolean)
  .join(' ')}
```

Replace with:
```typescript
className={[
  'flex-1 text-sm',
  task.priority === 'high' ? 'font-semibold text-foreground' : 'font-normal',
  task.priority === 'low' ? 'text-muted-foreground' : 'text-foreground',
]
  .filter(Boolean)
  .join(' ')}
```

This gives: High = bold + foreground, Medium = normal + foreground (falls through), Low = normal + muted-foreground.

Do NOT change the overdue badge classes (`text-xs text-muted-foreground bg-muted rounded px-2 py-0.5`) — the `bg-muted` badge on a `bg-background` row provides correct subtle contrast.
Do NOT touch the checkbox button classes.
  </action>
  <verify>
    <automated>grep -c "bg-muted" src/renderer/components/TaskRow.tsx | grep "^1$" && grep -c "bg-background" src/renderer/components/TaskRow.tsx | grep "^1$" && grep -c "border-l-\[3px\]" src/renderer/components/TaskRow.tsx | grep "^0$" && grep -c "opacity-60" src/renderer/components/TaskRow.tsx | grep "^0$" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - TaskRow.tsx outer div contains `bg-background` (not `bg-muted` for the row — `bg-muted` should only appear once, on the overdue badge)
    - TaskRow.tsx outer div contains `hover:bg-muted/40` (not `hover:bg-accent`)
    - TaskRow.tsx outer div contains `py-3` (not `py-4`)
    - TaskRow.tsx does NOT contain `border-l-[3px]` anywhere
    - TaskRow.tsx does NOT contain `border-primary` anywhere
    - TaskRow.tsx does NOT contain `border-transparent` anywhere
    - TaskRow.tsx does NOT contain `opacity-60` anywhere
    - TaskRow.tsx title span contains `text-muted-foreground` conditional for low priority
    - TaskRow.tsx title span contains `font-semibold text-foreground` conditional for high priority
    - npm test exits 0 (no backend regressions)
    - **Indigo audit (D-05, D-06):** Run `grep -rn "text-primary\|border-primary" src/renderer/` and confirm matches are limited to (1) `src/renderer/App.tsx` active nav tab and (2) `src/renderer/components/ui/button.tsx` only. No other files should contain `text-primary` or `border-primary`. If residual matches exist in other files, remove them by replacing with neutral equivalents (`text-foreground`, `border-border`).
  </acceptance_criteria>
  <done>TaskRow uses clean white background, hairline bottom dividers, weight-only priority signals (bold for high, muted text for low), subtle hover state, no left border colors, no whole-row opacity. No indigo leakage outside allowed touchpoints.</done>
</task>

</tasks>

<verification>
- `npm test` passes (no backend regressions)
- `grep "bg-muted" src/renderer/components/TaskRow.tsx` returns exactly 1 match (overdue badge only)
- `grep "border-l-\[3px\]" src/renderer/components/TaskRow.tsx` returns 0 matches
- `grep "opacity-60" src/renderer/components/TaskRow.tsx` returns 0 matches
- `grep "214.3 20% 93%" src/renderer/index.css` returns 1 match
- `grep -rn "text-primary\|border-primary" src/renderer/` returns matches only in App.tsx and button.tsx
</verification>

<success_criteria>
TaskRow renders with clean white background, hairline dividers, weight-only priority, and subtle hover. The --border CSS variable is lightened for all screens. No text-primary or border-primary usage outside of the Add Task button and active nav tab.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-ui-polish-minimalist-redesign-across-all-screens/04.1-01-SUMMARY.md`
</output>
