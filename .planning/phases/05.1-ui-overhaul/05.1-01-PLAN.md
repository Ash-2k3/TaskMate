---
phase: 05.1-ui-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/index.css
  - tailwind.config.js
autonomous: true
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-13
  - D-14
  - D-15
must_haves:
  truths:
    - "App renders all text in Inter font (not system default)"
    - "Tabular numbers align vertically in WeeklySummary stats"
    - "Text appears antialiased and crisp on macOS"
    - "text-2xs and text-ui Tailwind classes are available for use"
    - "Foreground text has a warm near-black tone (not blue-tinted)"
    - "Dark mode borders are subtly visible (not invisible)"
  artifacts:
    - path: "src/renderer/index.css"
      provides: "Inter font imports, font-feature-settings, antialiasing, warmer foreground token, dark border fix"
      contains: "@fontsource/inter"
    - path: "tailwind.config.js"
      provides: "Inter as sans font, text-2xs and text-ui custom sizes"
      contains: "Inter"
  key_links:
    - from: "src/renderer/index.css"
      to: "tailwind.config.js"
      via: "fontFamily.sans references Inter which index.css imports"
      pattern: "Inter"
---

<objective>
Install Inter font and establish the typography + color token foundation that all subsequent Wave 2 screen changes depend on.

Purpose: Every visual change in Wave 2 uses `text-2xs`, `text-ui`, or depends on Inter rendering correctly. This must land first.
Output: Inter font loaded offline, font-feature-settings active, antialiasing enabled, two custom Tailwind font size tokens, warmer foreground token, dark mode border fix.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-ui-overhaul/05.1-CONTEXT.md
@.planning/phases/05.1-ui-overhaul/05.1-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Inter font and configure index.css</name>
  <files>src/renderer/index.css</files>
  <read_first>src/renderer/index.css, .planning/phases/05.1-ui-overhaul/05.1-CONTEXT.md</read_first>
  <action>
1. Run `npm install @fontsource/inter` to install the offline-safe font package (per D-01).

2. In `src/renderer/index.css`, add three font imports at the very top of the file, BEFORE the `@tailwind` directives:
```
@import '@fontsource/inter/400.css';
@import '@fontsource/inter/500.css';
@import '@fontsource/inter/600.css';
```
Do NOT import weight 300 — it renders poorly in Electron's Chromium (per D-02).

3. Inside the FIRST `@layer base { :root { ... } }` block (the one with CSS variable definitions), add these three properties anywhere inside `:root`:
```
font-feature-settings: 'cv11' 1, 'tnum' 1, 'liga' 1, 'calt' 1;
-webkit-font-smoothing: antialiased;
-moz-osx-font-smoothing: grayscale;
```
Per D-03 and D-04.

4. In the same `:root` block, change the `--foreground` value:
  - BEFORE: `--foreground: 222.2 84% 4.9%;`
  - AFTER:  `--foreground: 220 15% 8%;`
Per D-13 (warmer near-black, less blue-tinted).

5. In the `.dark` block, change the `--border` value:
  - BEFORE: `--border: 217.2 32.6% 17.5%;`
  - AFTER:  `--border: 217.2 32.6% 22%;`
Per D-14 (borders visible in dark mode — currently same as --muted, making them invisible).

6. Do NOT change any other CSS variable values. Per D-15, all other color tokens stay unchanged.
  </action>
  <verify>
    <automated>cd /Users/ashwath/Desktop/Personal/AuctionX && grep -q "@fontsource/inter/400.css" src/renderer/index.css && grep -q "font-feature-settings" src/renderer/index.css && grep -q "webkit-font-smoothing" src/renderer/index.css && grep -q "220 15% 8%" src/renderer/index.css && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - src/renderer/index.css contains `@import '@fontsource/inter/400.css'`
    - src/renderer/index.css contains `@import '@fontsource/inter/500.css'`
    - src/renderer/index.css contains `@import '@fontsource/inter/600.css'`
    - src/renderer/index.css contains `font-feature-settings: 'cv11' 1, 'tnum' 1, 'liga' 1, 'calt' 1`
    - src/renderer/index.css contains `-webkit-font-smoothing: antialiased`
    - src/renderer/index.css contains `-moz-osx-font-smoothing: grayscale`
    - src/renderer/index.css contains `--foreground: 220 15% 8%`
    - src/renderer/index.css .dark block contains `--border: 217.2 32.6% 22%`
    - package.json contains `@fontsource/inter` in dependencies
    - No `@fontsource/inter/300.css` import exists
  </acceptance_criteria>
  <done>Inter 400/500/600 imported, font features active, antialiasing enabled, foreground warmer, dark border visible</done>
</task>

<task type="auto">
  <name>Task 2: Extend Tailwind config with Inter font family and custom sizes</name>
  <files>tailwind.config.js</files>
  <read_first>tailwind.config.js, .planning/phases/05.1-ui-overhaul/05.1-CONTEXT.md</read_first>
  <action>
In `tailwind.config.js`, inside the `theme.extend` object, add two new keys alongside the existing `colors` and `borderRadius`:

1. Add `fontFamily` (per D-05):
```js
fontFamily: {
  sans: ['Inter', 'system-ui', '-apple-system', 'BlinkMacSystemFont', 'sans-serif'],
},
```

2. Add `fontSize` (per D-06):
```js
fontSize: {
  '2xs': ['11px', { lineHeight: '1rem' }],
  'ui': ['13px', { lineHeight: '1.25rem' }],
},
```

The final `theme.extend` object should have these keys in order: `colors`, `borderRadius`, `fontFamily`, `fontSize`. Do NOT remove or modify the existing `colors` or `borderRadius` entries.
  </action>
  <verify>
    <automated>cd /Users/ashwath/Desktop/Personal/AuctionX && grep -q "'Inter'" tailwind.config.js && grep -q "'2xs'" tailwind.config.js && grep -q "'ui'" tailwind.config.js && npx tsc --noEmit 2>&1 | head -5 && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - tailwind.config.js contains `'Inter'` in fontFamily.sans
    - tailwind.config.js contains `'2xs': ['11px', { lineHeight: '1rem' }]`
    - tailwind.config.js contains `'ui': ['13px', { lineHeight: '1.25rem' }]`
    - Existing `colors` and `borderRadius` entries are unchanged
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Tailwind config extended with Inter as default sans font, text-2xs (11px) and text-ui (13px) available as utility classes</done>
</task>

</tasks>

<verification>
1. `npm test` passes (no JS regressions)
2. `grep -r "transition\|duration-\|animate-" src/renderer/ --include="*.tsx" --include="*.css"` shows no NEW animation classes (pre-existing `transition-colors` on time input is pre-existing, not introduced here)
3. `grep "'Inter'" tailwind.config.js` confirms font family
4. `grep "fontsource/inter" src/renderer/index.css` confirms 3 weight imports
</verification>

<success_criteria>
- Inter font package installed as a dependency
- index.css has 3 weight imports (400, 500, 600), font-feature-settings, antialiasing
- tailwind.config.js has Inter as sans font, text-2xs and text-ui custom sizes
- Foreground token warmed, dark border token lightened
- All existing tests pass
- No animation/transition classes introduced
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-ui-overhaul/05.1-01-SUMMARY.md`
</output>
