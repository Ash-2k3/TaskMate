---
phase: 05.2-glassmorphism-dark-theme
plan: 02
type: execute
wave: 2
depends_on: ["05.2-01"]
files_modified:
  - src/renderer/components/TaskRow.tsx
  - src/renderer/screens/TodayView.tsx
  - src/renderer/components/EmptyState.tsx
  - src/renderer/components/ReflectionModal.tsx
  - src/renderer/components/ui/calendar.tsx
autonomous: true
requirements:
  - D-23
  - D-25
  - D-26
  - D-31
  - D-32
  - D-33
  - D-34
  - D-37
  - D-38
  - D-39

must_haves:
  truths:
    - "Each task row is a glass card that lifts on hover"
    - "TodayView header is a glass card with rounded corners and margin"
    - "Empty state text sits inside a glass card"
    - "Reflection modal has frosted glass background with stronger blur"
    - "DatePicker calendar renders correctly with v9 classNames and dark glass palette"
  artifacts:
    - path: "src/renderer/components/TaskRow.tsx"
      provides: "Glass card task row with hover lift"
      contains: "glass glass-hover"
    - path: "src/renderer/screens/TodayView.tsx"
      provides: "Glass header card, updated task list spacing"
      contains: "glass rounded-2xl"
    - path: "src/renderer/components/EmptyState.tsx"
      provides: "Glass empty state card"
      contains: "glass rounded-2xl"
    - path: "src/renderer/components/ReflectionModal.tsx"
      provides: "Glass modal with 20px blur"
      contains: "blur(20px)"
    - path: "src/renderer/components/ui/calendar.tsx"
      provides: "v9 classNames remap with dark glass styling"
      contains: "month_caption"
  key_links:
    - from: "src/renderer/components/TaskRow.tsx"
      to: "src/renderer/index.css"
      via: ".glass .glass-hover utility classes"
      pattern: "glass glass-hover"
    - from: "src/renderer/components/ui/calendar.tsx"
      to: "react-day-picker v9"
      via: "v9 classNames keys"
      pattern: "day_button"
---

<objective>
Apply glass treatment to the core components on the main screen: TaskRow (glass cards with hover), TodayView header (glass card), EmptyState (glass wrapper), ReflectionModal (glass dialog), and calendar.tsx (v9 classNames remap + dark glass styling).

Purpose: The primary user surface (Today view + task interactions + calendar + reflection) gets the full glassmorphism treatment.
Output: Main screen components rendered as glass surfaces with correct hover states, and calendar functioning correctly with v9 API.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.2-glassmorphism-dark-theme/05.2-CONTEXT.md
@.planning/phases/05.2-glassmorphism-dark-theme/05.2-RESEARCH.md
@.planning/phases/05.2-glassmorphism-dark-theme/05.2-01-SUMMARY.md

<interfaces>
From src/renderer/index.css (created in Plan 01):
- `.glass` — background: rgba(255,255,255,0.05); backdrop-filter: blur(16px); border: 1px solid rgba(255,255,255,0.10)
- `.glass-hover` — transition on hover to rgba(255,255,255,0.09) bg + cyan border + translateY(-2px)
- `.transition-glass` — transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TaskRow glass cards, TodayView glass header, and EmptyState glass wrapper</name>
  <files>src/renderer/components/TaskRow.tsx, src/renderer/screens/TodayView.tsx, src/renderer/components/EmptyState.tsx</files>
  <read_first>
    - src/renderer/components/TaskRow.tsx (current: "flex items-center gap-3 px-4 py-3 bg-background border-b border-border cursor-pointer hover:bg-muted/40")
    - src/renderer/screens/TodayView.tsx (current header: div with px-6 pt-6 pb-4 border-b border-border; task list: div with mx-6)
    - src/renderer/components/EmptyState.tsx (current: "px-4 py-8" wrapper div)
  </read_first>
  <action>
1. **TaskRow.tsx** (D-26, D-31, D-16): Change the outer div className from:
```
"flex items-center gap-3 px-4 py-3 bg-background border-b border-border cursor-pointer hover:bg-muted/40"
```
to:
```
"glass glass-hover flex items-center gap-3 px-4 py-3 rounded-lg cursor-pointer mb-2"
```
Key changes:
- Added `glass glass-hover` (glass surface + hover effect with translateY(-2px) and cyan border glow)
- Removed `bg-background` (glass provides its own semi-transparent background)
- Removed `border-b border-border` (glass provides its own white/10 border)
- Removed `hover:bg-muted/40` (glass-hover provides the hover state)
- Added `rounded-lg` (8px per D-16 for rows)
- Added `mb-2` (spacing between card rows, replaces border-b separator pattern)

2. **TodayView.tsx** (D-32): Change the header div from:
```
<div className="flex items-start justify-between px-6 pt-6 pb-4 border-b border-border">
```
to:
```
<div className="glass rounded-2xl flex items-start justify-between mx-4 mt-4 mb-0 px-4 py-4">
```
Key changes:
- Added `glass rounded-2xl` (16px glass card per D-32)
- Changed padding from `px-6 pt-6 pb-4` to `px-4 py-4` (card has its own internal padding)
- Changed from edge-to-edge to `mx-4 mt-4 mb-0` (inset with margin)
- Removed `border-b border-border` (glass provides its own border)

3. **TodayView.tsx** — Change the task list container from:
```
<div className="mx-6">
```
to:
```
<div className="px-4 pt-2">
```
This gives the task rows (now glass cards with mb-2) proper padding and a small gap from the header.

4. **EmptyState.tsx** (D-37): Change the outer div className from:
```
"px-4 py-8"
```
to:
```
"glass rounded-2xl px-4 py-6 mx-4 mt-4"
```
This wraps the empty state text in a glass card with 16px corners. The `mx-4 mt-4` matches the header card inset pattern. Changed py-8 to py-6 for tighter card.
  </action>
  <verify>
    <automated>cd /Users/ashwath/Desktop/Personal/AuctionX && grep -q 'glass glass-hover' src/renderer/components/TaskRow.tsx && grep -q 'rounded-lg cursor-pointer mb-2' src/renderer/components/TaskRow.tsx && grep -qv 'bg-background' src/renderer/components/TaskRow.tsx && grep -qv 'border-b border-border' src/renderer/components/TaskRow.tsx && grep -q 'glass rounded-2xl' src/renderer/screens/TodayView.tsx && grep -q 'glass rounded-2xl' src/renderer/components/EmptyState.tsx && npx tsc --noEmit && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - TaskRow.tsx outer div className contains `glass glass-hover` and `rounded-lg` and `mb-2`
    - TaskRow.tsx outer div className does NOT contain `bg-background` or `border-b border-border` or `hover:bg-muted/40`
    - TodayView.tsx header div className contains `glass rounded-2xl` and `mx-4 mt-4`
    - TodayView.tsx header div does NOT contain `border-b border-border`
    - TodayView.tsx task list div has `px-4 pt-2` (not `mx-6`)
    - EmptyState.tsx outer div className contains `glass rounded-2xl px-4 py-6`
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>TaskRow renders as individual glass cards with hover lift, TodayView header is a glass card panel, EmptyState wraps in a glass card. No nested backdrop-filter issues (all glass surfaces are siblings, not parent-child).</done>
</task>

<task type="auto">
  <name>Task 2: ReflectionModal glass dialog and calendar.tsx v9 classNames remap</name>
  <files>src/renderer/components/ReflectionModal.tsx, src/renderer/components/ui/calendar.tsx</files>
  <read_first>
    - src/renderer/components/ReflectionModal.tsx (current DialogContent className: "sm:max-w-lg")
    - src/renderer/components/ui/dialog.tsx (DialogContent base class includes "border bg-background")
    - src/renderer/components/ui/calendar.tsx (current v8 classNames: caption, head_row, head_cell, row, cell, day, day_selected, etc.)
    - .planning/phases/05.2-glassmorphism-dark-theme/05.2-RESEARCH.md (react-day-picker v9 classNames Complete Remap section, ReflectionModal Glass Override section)
  </read_first>
  <action>
1. **ReflectionModal.tsx** (D-33): Change the `DialogContent` element from:
```tsx
<DialogContent
  className="sm:max-w-lg"
```
to:
```tsx
<DialogContent
  className="sm:max-w-lg glass"
  style={{ backdropFilter: 'blur(20px)', WebkitBackdropFilter: 'blur(20px)' }}
```
The inline `style` overrides `.glass`'s default 16px blur to 20px for modal elevation per D-33. The `.glass` class provides the background rgba and border. The DialogContent base class in dialog.tsx has `border bg-background` — the `.glass` class's `border: 1px solid rgba(255,255,255,0.10)` will be layered; add `border-white/10 bg-transparent` to override the base:
```tsx
<DialogContent
  className="sm:max-w-lg glass border-white/10 bg-transparent"
  style={{ backdropFilter: 'blur(20px)', WebkitBackdropFilter: 'blur(20px)' }}
```

Also update the textarea styling inside the modal to use glass-compatible colors. Change `bg-background` to `bg-white/5` in the textarea className:
```
"w-full min-h-[80px] rounded-md border border-white/10 bg-white/5 px-3 py-2 text-sm resize-y focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring"
```

2. **calendar.tsx** (D-38, D-39): Replace the entire `classNames` object with the v9 API remap. The full replacement:

Remove the old v8 keys: `caption`, `nav_button`, `nav_button_previous`, `nav_button_next`, `table`, `head_row`, `head_cell`, `row`, `cell`, `day`, `day_selected`, `day_today`, `day_outside`, `day_disabled`, `day_range_end`, `day_range_middle`, `day_hidden`.

Replace with v9 keys:
```tsx
classNames={{
  months: 'flex flex-col space-y-4',
  month: 'space-y-4',
  month_caption: 'flex justify-center pt-1 relative items-center',
  caption_label: 'text-sm font-medium text-foreground',
  nav: 'space-x-1 flex items-center',
  button_previous: cn(
    buttonVariants({ variant: 'outline' }),
    'h-7 w-7 bg-transparent p-0 opacity-50 hover:opacity-100 absolute left-1 border-white/10'
  ),
  button_next: cn(
    buttonVariants({ variant: 'outline' }),
    'h-7 w-7 bg-transparent p-0 opacity-50 hover:opacity-100 absolute right-1 border-white/10'
  ),
  month_grid: 'w-full border-collapse space-y-1',
  weekdays: 'flex w-full',
  weekday: 'text-muted-foreground rounded-md flex-1 text-center font-normal text-[0.8rem]',
  weeks: 'w-full',
  week: 'flex w-full mt-2',
  day: 'flex-1 h-9 text-center text-sm p-0 relative focus-within:relative focus-within:z-20',
  day_button: cn(
    buttonVariants({ variant: 'ghost' }),
    'h-9 w-full p-0 font-normal aria-selected:opacity-100'
  ),
  selected: 'bg-primary text-primary-foreground hover:bg-primary hover:text-primary-foreground focus:bg-primary focus:text-primary-foreground rounded-md',
  today: 'bg-accent text-accent-foreground rounded-md',
  outside: 'text-muted-foreground opacity-50',
  disabled: 'text-muted-foreground opacity-50',
  range_end: 'day-range-end',
  range_middle: 'aria-selected:bg-accent aria-selected:text-accent-foreground',
  hidden: 'invisible',
  ...classNames,
}}
```

Also wrap the `DayPicker` in a glass-styled container by changing the `className` from `cn('p-3', className)` to `cn('p-3 glass rounded-2xl', className)` per D-34.
  </action>
  <verify>
    <automated>cd /Users/ashwath/Desktop/Personal/AuctionX && grep -q 'glass' src/renderer/components/ReflectionModal.tsx && grep -q 'blur(20px)' src/renderer/components/ReflectionModal.tsx && grep -q 'month_caption' src/renderer/components/ui/calendar.tsx && grep -q 'day_button' src/renderer/components/ui/calendar.tsx && grep -q 'button_previous' src/renderer/components/ui/calendar.tsx && grep -q 'weekdays' src/renderer/components/ui/calendar.tsx && grep -qv 'head_row' src/renderer/components/ui/calendar.tsx && grep -qv 'head_cell' src/renderer/components/ui/calendar.tsx && npx tsc --noEmit && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - ReflectionModal.tsx DialogContent className contains `glass` and `border-white/10` and `bg-transparent`
    - ReflectionModal.tsx DialogContent has inline style with `backdropFilter: 'blur(20px)'`
    - ReflectionModal.tsx textarea className contains `bg-white/5` and `border-white/10` (not `bg-background` and `border-input`)
    - calendar.tsx classNames object contains v9 keys: `month_caption`, `day_button`, `button_previous`, `button_next`, `month_grid`, `weekdays`, `weekday`, `weeks`, `week`, `day`, `selected`, `today`, `outside`, `disabled`, `hidden`
    - calendar.tsx does NOT contain v8 keys: `head_row`, `head_cell`, `nav_button_previous` (as standalone key), `table`, `day_selected`, `day_today`, `day_outside`, `day_disabled`, `day_hidden`
    - calendar.tsx DayPicker className contains `glass rounded-2xl`
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>ReflectionModal appears as frosted glass with 20px blur. Calendar renders correctly with v9 classNames API and dark glass palette. No v8 classNames remain.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (TypeScript safety, catches bad v9 classNames)
- All v8 calendar keys removed, all v9 keys present
- Glass classes applied to TaskRow, TodayView header, EmptyState, ReflectionModal, Calendar
- No nested backdrop-filter: TaskRow glass cards are siblings inside a non-glass wrapper div; ReflectionModal renders via portal (not nested)
</verification>

<success_criteria>
- TaskRow renders as glass card with hover lift and cyan border glow
- TodayView header is a glass card with rounded-2xl corners
- EmptyState wraps in a glass card
- ReflectionModal has glass with 20px blur override
- Calendar uses v9 classNames API correctly
- Calendar renders inside a glass container
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-glassmorphism-dark-theme/05.2-02-SUMMARY.md`
</output>
