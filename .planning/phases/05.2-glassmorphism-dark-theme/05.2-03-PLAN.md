---
phase: 05.2-glassmorphism-dark-theme
plan: 03
type: execute
wave: 2
depends_on: ["05.2-01"]
files_modified:
  - src/renderer/screens/AddTask.tsx
  - src/renderer/screens/EditTask.tsx
  - src/renderer/screens/WeeklySummary.tsx
  - src/renderer/screens/Settings.tsx
  - src/renderer/screens/ReflectionsHistory.tsx
  - src/main/reminder-scheduler.ts
autonomous: true
requirements:
  - D-35
  - D-36
  - D-40
  - D-41
  - D-42
  - D-43

must_haves:
  truths:
    - "AddTask form is wrapped in a glass card"
    - "EditTask form is wrapped in a glass card"
    - "WeeklySummary sections are each wrapped in glass cards"
    - "Settings sections are wrapped in glass cards"
    - "ReflectionsHistory rows sit inside a glass card"
    - "Notification bodies use personal/warm tone with emojis"
    - "Reflection prompt fires a system notification alongside the IPC message"
  artifacts:
    - path: "src/renderer/screens/AddTask.tsx"
      provides: "Glass form card"
      contains: "glass rounded-2xl"
    - path: "src/renderer/screens/EditTask.tsx"
      provides: "Glass form card"
      contains: "glass rounded-2xl"
    - path: "src/renderer/screens/WeeklySummary.tsx"
      provides: "Glass section cards"
      contains: "glass rounded-2xl"
    - path: "src/renderer/screens/Settings.tsx"
      provides: "Glass section cards"
      contains: "glass rounded-2xl"
    - path: "src/renderer/screens/ReflectionsHistory.tsx"
      provides: "Glass list card with hover rows"
      contains: "glass rounded-2xl"
    - path: "src/main/reminder-scheduler.ts"
      provides: "Updated notification copy and new reflection notification"
      contains: "Time to reflect on your day"
  key_links:
    - from: "src/main/reminder-scheduler.ts"
      to: "Electron Notification API"
      via: "new Notification for reflection prompt"
      pattern: "Time to reflect on your day"
---

<objective>
Apply glass treatment to all secondary screens (AddTask, EditTask, WeeklySummary, Settings, ReflectionsHistory) and update notification copy to personal/warm tone with new reflection system notification.

Purpose: Complete the glassmorphism visual overhaul across every screen and update notification personality.
Output: All screens use glass cards, notifications have warm copy with emojis, reflection prompt fires a system notification.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.2-glassmorphism-dark-theme/05.2-CONTEXT.md
@.planning/phases/05.2-glassmorphism-dark-theme/05.2-RESEARCH.md
@.planning/phases/05.2-glassmorphism-dark-theme/05.2-01-SUMMARY.md

<interfaces>
From src/renderer/index.css (created in Plan 01):
- `.glass` — background: rgba(255,255,255,0.05); backdrop-filter: blur(16px); border: 1px solid rgba(255,255,255,0.10)
- `.glass-hover` — hover state with translateY(-2px) and cyan border
- `.transition-glass` — transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Glass cards on AddTask, EditTask, WeeklySummary, Settings, and ReflectionsHistory</name>
  <files>src/renderer/screens/AddTask.tsx, src/renderer/screens/EditTask.tsx, src/renderer/screens/WeeklySummary.tsx, src/renderer/screens/Settings.tsx, src/renderer/screens/ReflectionsHistory.tsx</files>
  <read_first>
    - src/renderer/screens/AddTask.tsx (form body: div with className="space-y-4", wrapped in min-h-screen bg-background px-6 py-6)
    - src/renderer/screens/EditTask.tsx (same structure as AddTask)
    - src/renderer/screens/WeeklySummary.tsx (three section divs each with className="mb-6")
    - src/renderer/screens/Settings.tsx (stats div with space-y-2 mb-8, export div with mb-6, delete div with border-t)
    - src/renderer/screens/ReflectionsHistory.tsx (reflections list: div wrapping mapped rows with border-b border-border)
  </read_first>
  <action>
1. **AddTask.tsx** (D-35): Wrap the form `<div className="space-y-4">` in a glass card. Change from:
```tsx
<div className="space-y-4">
```
to:
```tsx
<div className="glass rounded-2xl p-6 space-y-4">
```
And remove the `px-6` from the outer wrapper since the glass card provides its own padding. Change the outer div from:
```
"min-h-screen bg-background px-6 py-6"
```
to:
```
"min-h-screen bg-background px-4 py-6"
```
Keep the back button and h1 OUTSIDE the glass card (they sit above it). The glass card wraps only the form fields + save button. Adjust: move the back button and h1 to use `px-2` internal padding (since outer already has px-4) so they stay flush:

Actually, simpler approach — keep the outer px-6 py-6 but wrap ONLY the form body `<div className="space-y-4">` in the glass card:
```tsx
{/* Form */}
<div className="glass rounded-2xl p-6">
  <div className="space-y-4">
    {/* ...form fields... */}
  </div>
</div>
```
This keeps back button and title flush-left in px-6 and wraps only the form.

2. **EditTask.tsx** (D-35): Same treatment as AddTask. Wrap the form `<div className="space-y-4">` in:
```tsx
<div className="glass rounded-2xl p-6">
  <div className="space-y-4">
    {/* ...form fields + save button + delete section... */}
  </div>
</div>
```

3. **WeeklySummary.tsx** (D-36): Wrap each of the three section blocks in glass cards. Change each section from `<div className="mb-6">` to `<div className="glass rounded-2xl p-4 mb-4">`.

Specifically:
- "This week" section: `<div className="mb-6">` becomes `<div className="glass rounded-2xl p-4 mb-4">`
- "Still waiting" section: `<div className="mb-6">` becomes `<div className="glass rounded-2xl p-4 mb-4">`
- "Recurring topic" section: `<div className="mb-6">` becomes `<div className="glass rounded-2xl p-4 mb-4">`

Also update the week selector `<select>` element border from `border-border` to `border-white/10`:
```
className="appearance-none font-sans text-ui text-muted-foreground bg-transparent border border-white/10 rounded px-2 py-1 mb-6 cursor-pointer"
```
Changed `bg-background` to `bg-transparent` and `border-border` to `border-white/10`.

4. **Settings.tsx** (D-36): Wrap the stats block and export block in glass cards.

Stats block: Change from `<div className="space-y-2 mb-8">` to:
```tsx
<div className="glass rounded-2xl p-4 mb-4">
  <div className="space-y-2">
    {/* ...stats... */}
  </div>
</div>
```

Export block: Change from `<div className="mb-6">` to:
```tsx
<div className="glass rounded-2xl p-4 mb-4">
  {/* ...export button + status... */}
</div>
```

Delete section: Change from `<div className="border-t border-border pt-6">` to:
```tsx
<div className="glass rounded-2xl p-4">
  {/* ...delete button/confirmation... */}
</div>
```
Remove `border-t border-border pt-6` (glass card provides its own visual separation).

5. **ReflectionsHistory.tsx** (D-36): Wrap the reflections list in a glass card. Change from:
```tsx
<div>
  {reflections.map((r: ReflectionRecord) => (
    <div key={r.date} className="border-b border-border">
```
to:
```tsx
<div className="glass rounded-2xl overflow-hidden mx-4">
  {reflections.map((r: ReflectionRecord) => (
    <div key={r.date} className="border-b border-white/10">
```
The `overflow-hidden` ensures the rounded corners clip inner row borders. Changed `border-border` to `border-white/10` for glass-consistent borders.

Update the row hover from `hover:bg-muted/40` to `hover:bg-white/5`:
```tsx
<button
  className="w-full text-left px-4 py-3 font-medium hover:bg-white/5 transition-glass"
```

Also update the heading and list wrapper. Change outer padding from `px-0 pt-6` to `px-4 pt-6` for consistent spacing with the glass card:
```tsx
<div className="pt-6">
  <h1 className="text-2xl font-semibold mb-6 px-4">Reflections</h1>
```
Actually, keep the heading at px-6 but the glass card at mx-4. The heading sits above the card:
```tsx
<div className="pt-6">
  <h1 className="text-2xl font-semibold mb-4 px-6">Reflections</h1>
  <div className="glass rounded-2xl overflow-hidden mx-4">
    {reflections.map(...)}
  </div>
</div>
```
  </action>
  <verify>
    <automated>cd /Users/ashwath/Desktop/Personal/AuctionX && grep -q 'glass rounded-2xl p-6' src/renderer/screens/AddTask.tsx && grep -q 'glass rounded-2xl p-6' src/renderer/screens/EditTask.tsx && grep -q 'glass rounded-2xl p-4 mb-4' src/renderer/screens/WeeklySummary.tsx && grep -q 'glass rounded-2xl p-4' src/renderer/screens/Settings.tsx && grep -q 'glass rounded-2xl overflow-hidden' src/renderer/screens/ReflectionsHistory.tsx && npx tsc --noEmit && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - AddTask.tsx form body wrapped in div with `glass rounded-2xl p-6`
    - EditTask.tsx form body wrapped in div with `glass rounded-2xl p-6`
    - WeeklySummary.tsx each of 3 section divs has `glass rounded-2xl p-4 mb-4`
    - WeeklySummary.tsx select element uses `border-white/10` and `bg-transparent` (not `border-border` or `bg-background`)
    - Settings.tsx stats, export, and delete sections each wrapped in `glass rounded-2xl p-4`
    - Settings.tsx no longer has `border-t border-border` on delete section
    - ReflectionsHistory.tsx reflections list wrapped in `glass rounded-2xl overflow-hidden`
    - ReflectionsHistory.tsx row borders use `border-white/10` (not `border-border`)
    - ReflectionsHistory.tsx row hover uses `hover:bg-white/5` (not `hover:bg-muted/40`)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>All secondary screens display glass cards for their content sections. Glass card pattern is consistent across the app.</done>
</task>

<task type="auto">
  <name>Task 2: Notification copy rewrite and new reflection system notification</name>
  <files>src/main/reminder-scheduler.ts</files>
  <read_first>
    - src/main/reminder-scheduler.ts (4 notification sites: lines 34-37 initial reminder, lines 54-57 re-notification, lines 111-114 weekly summary, lines 76-83 reflection trigger)
    - .planning/phases/05.2-glassmorphism-dark-theme/05.2-RESEARCH.md (Notification Copy Pattern section, reflection-scheduler.ts code example)
  </read_first>
  <action>
1. **Initial reminder notification (D-40):** Change lines 34-37 from:
```typescript
const notification = new Notification({
  title: 'TaskMate Reminder',
  body: task.title,
});
```
to:
```typescript
const notification = new Notification({
  title: 'TaskMate',
  body: `Hey, ${task.title} is waiting on you \u{1F440}`,
});
```

2. **Re-notification (D-41):** Change lines 54-57 from:
```typescript
const notification = new Notification({
  title: 'TaskMate Reminder',
  body: `Still incomplete \u2014 ${task.title}`,
});
```
to:
```typescript
const notification = new Notification({
  title: 'TaskMate',
  body: `Still pending: ${task.title} \u2014 you've got this \u{1F4AA}`,
});
```

3. **Weekly summary notification (D-42):** Change lines 111-113 from:
```typescript
new Notification({
  title: 'TaskMate',
  body: 'Your weekly summary is ready',
}).show();
```
to:
```typescript
new Notification({
  title: 'TaskMate',
  body: 'Your week in review is ready \u2014 see how you did \u2728',
}).show();
```

4. **New reflection system notification (D-43):** Inside the reflection trigger block (around line 78), add a new Notification BEFORE the `win.webContents.send('prompt:reflection')` line. The code should look like:
```typescript
if (snoozePassed && reflectionFiredToday !== todayDate) {
  const win = getMainWindow();
  if (win && !win.isDestroyed()) {
    // New system notification for reflection (D-43)
    const reflectionNotif = new Notification({
      title: 'TaskMate',
      body: 'Time to reflect on your day \u{1F319}',
    });
    reflectionNotif.on('click', () => {
      if (win && !win.isDestroyed()) {
        win.show();
        win.focus();
      }
    });
    reflectionNotif.show();
    // Existing IPC message
    win.webContents.send('prompt:reflection');
    reflectionFiredToday = todayDate;
  }
}
```
The click handler uses the same `win.show(); win.focus()` pattern already used by task reminder notifications (lines 38-44).
  </action>
  <verify>
    <automated>cd /Users/ashwath/Desktop/Personal/AuctionX && grep -q 'is waiting on you' src/main/reminder-scheduler.ts && grep -q "you've got this" src/main/reminder-scheduler.ts && grep -q 'see how you did' src/main/reminder-scheduler.ts && grep -q 'Time to reflect on your day' src/main/reminder-scheduler.ts && grep -q 'reflectionNotif.show()' src/main/reminder-scheduler.ts && npx tsc --noEmit && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - Initial reminder body contains `Hey, ${task.title} is waiting on you` with eyes emoji
    - Initial reminder title is `TaskMate` (not `TaskMate Reminder`)
    - Re-notification body contains `Still pending: ${task.title}` and `you've got this` with flexed bicep emoji
    - Re-notification title is `TaskMate` (not `TaskMate Reminder`)
    - Weekly summary body contains `Your week in review is ready` and `see how you did` with sparkles emoji
    - New reflection notification exists with body `Time to reflect on your day` and crescent moon emoji
    - Reflection notification has click handler with `win.show()` and `win.focus()`
    - Reflection notification fires BEFORE `win.webContents.send('prompt:reflection')`
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>All 4 notification sites updated with warm personal copy and emojis. New reflection system notification fires alongside the IPC message with click-to-focus handler.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- All secondary screens have glass card wrappers (grep for `glass rounded-2xl`)
- All 4 notification bodies updated with warm copy
- New reflection notification exists with click handler
</verification>

<success_criteria>
- AddTask and EditTask forms wrapped in glass cards
- WeeklySummary, Settings, and ReflectionsHistory sections wrapped in glass cards
- ReflectionsHistory uses glass-compatible borders and hover
- All 3 existing notifications have warm personal copy with emojis
- New reflection system notification fires at 9 PM alongside IPC message
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-glassmorphism-dark-theme/05.2-03-SUMMARY.md`
</output>
